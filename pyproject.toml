[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "taiwan-produce-watch"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
tpw = "tpw.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
